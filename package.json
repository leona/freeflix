{
  "name": "freeflix",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:api": "bun run --cwd apps/api dev",
    "dev:navigator": "bun run --cwd apps/navigator dev",
    "build:navigator": "bun run --cwd apps/navigator build",
    "build:api": "bun run --cwd apps/api build"
  }
}